services:
  - type: web
    name: python webhook_receiver
    env: python
    buildCommand: ""
    startCommand: "python webhook_receiver.py"
    plan: free
    autoDeploy: true
    envVars: []
    ports:
      - port: 5000
